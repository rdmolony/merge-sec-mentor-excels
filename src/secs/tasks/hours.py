import pandas as pd


def calculate_monthly_sec_activity_days(sec_activities: pd.DataFrame) -> pd.DataFrame:

    import ipdb

    ipdb.set_trace()


def calculate_monthly_other_activity_days(
    other_activities: pd.DataFrame,
) -> pd.DataFrame:
    pass
